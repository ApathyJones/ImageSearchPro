# requirements-mac.txt — macOS Apple Silicon (M1/M2/M3/M4)
# Note: Intel Mac is capped at torch 2.2.x; PyTorch dropped x86_64 macOS wheels after 2.2.
# For Windows/NVIDIA CUDA, use requirements.txt instead.

# ── GUI ───────────────────────────────────────────────────────────────────────
PyQt6>=6.5.0

# ── Core numerics & image I/O ─────────────────────────────────────────────────
Pillow==12.0.0
numpy==2.2.6
rawpy==0.25.1
opencv-python==4.13.0.92

# ── PyTorch (Apple MPS / CPU — standard wheel includes MPS since PyTorch 2.0) ─
torch>=2.6.0
torchvision>=0.21.0

# ── Vision models ─────────────────────────────────────────────────────────────
open_clip_torch==3.2.0
transformers>=4.56
huggingface_hub>=0.24
dinotool

# ── Utilities ─────────────────────────────────────────────────────────────────
psutil>=5.9
scikit-learn>=1.3
Send2Trash==2.1.0

# ── Optional: NSFW detection (enables the NSFW scan button) ──────────────────
# pip install nudenet

# ── Optional: face search via InsightFace ────────────────────────────────────
# Requires: brew install cmake
# pip install insightface onnxruntime
