--extra-index-url https://download.pytorch.org/whl/cu121

# ── GUI ───────────────────────────────────────────────────────────────────────
PyQt6>=6.5.0

# ── Core numerics & image I/O ─────────────────────────────────────────────────
Pillow==12.0.0
numpy==2.2.6
rawpy==0.25.1
opencv-python==4.13.0.92

# ── PyTorch (CUDA 12.8) ───────────────────────────────────────────────────────
torch==2.2.2+cu121
torchvision==0.17.2+cu121

# ── Xformers (CUDA 12.8) ───────────────────────────────────────────────────────
xformers

# ── Triton (CUDA 12.8) ───────────────────────────────────────────────────────
triton-windows

# ── Vision models ─────────────────────────────────────────────────────────────
open_clip_torch==3.2.0
transformers>=4.56
huggingface_hub>=0.24
dinotool

# ── Utilities ─────────────────────────────────────────────────────────────────
psutil>=5.9
scikit-learn>=1.3
Send2Trash==2.1.0

# ── Optional: NSFW detection (enables the NSFW scan button) ──────────────────
# pip install nudenet

# ── Optional: face search via InsightFace ────────────────────────────────────
# pip install insightface onnxruntime

# ── Optional: AMD GPU acceleration on Windows (DirectML) ─────────────────────
# pip install torch-directml

# ── Optional (legacy): ONNX GPU acceleration ─────────────────────────────────
# Only useful on older NVIDIA GPUs. Also requires code changes in PhotoSearchPro.py.
# pip install onnxruntime-gpu==1.23.2
